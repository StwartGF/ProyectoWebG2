@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}
<h1 class="text-danger">Error</h1>
<h2 class="text-danger">Ocurrió un error al procesar tu solicitud.</h2>
@if (Model.ShowRequestId)
{
    <p>
        <strong>ID de solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}
<h3>Modo desarrollo</h3>
<p>
    Cambiar al entorno <strong>Desarrollo</strong> mostrará información más detallada sobre el error ocurrido.
</p>
<p>
    <strong>No se debe habilitar el entorno de Desarrollo en aplicaciones desplegadas.</strong>
    Puede mostrar información sensible de excepciones a los usuarios finales.
    Para depuración local, habilita la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> como <strong>Development</strong>
    y reinicia la aplicación.
</p>
